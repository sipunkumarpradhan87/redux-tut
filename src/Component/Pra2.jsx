import React from 'react';

function DisplayPage() {
  // Assuming you have a way to receive the data, either as props or from state management
  const dataToDisplay = 'Data to display on another page';

  return (
    <div>
      <p>{dataToDisplay}</p>
    </div>
  );
}

export default DisplayPage;






